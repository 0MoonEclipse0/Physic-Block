/* Util.h defining Functions for the project */

/* Includes */
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/* Function to read a shader-file */
const char* readShader(const char* shaderfile,char buf[] , int bufsize);